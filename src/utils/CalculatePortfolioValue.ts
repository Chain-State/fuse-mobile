

// Get the asset portfolio of a user from (db:backend)
export const getPortfolio = async () => {

}

// TODO: Fetch current price for asset from api 


export const getTotalPortfolioValue =  async () => {

    // Get Individual Assets from Portfolio
    // Create an object (A) with assets and quantity as "key"
    // get Current price of each asset 
    // append price data to each asset in A 
    // Create a list of the objects A
    // For each object multiply the quantity with price and accumulate a total
    // return the total

}